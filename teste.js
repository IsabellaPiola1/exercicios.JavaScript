console.log("Fim do programa")

let i = 1; //variavel i, inicialização 

while (i < 5) /*condição*/ {
  console.log("Teste: ", i);

  i += 1; // passo, se for verdadeiro executa, não fica rodando pra sempre

  // i = Math.random(100); controla a quantidade de vezes que vai ser executada
}
